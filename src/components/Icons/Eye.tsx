import * as React from 'react'

type TypeIcons = {
	color?: string
	className?: string
}

 export const Eye = ({color = 'black', className}:TypeIcons) => {
	
	return (
		<div className={className}>

		</div>
	)
 }
